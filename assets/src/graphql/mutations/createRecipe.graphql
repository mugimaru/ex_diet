mutation createRecipe($input: CreateRecipeInput!) {
  updateRecipe(input: $input) {
    id
    name
    description
    weightCooked
    recipeIngredients {
      id
      weight
      ingredient {
        id
        name
        protein
        fat
        carbonhydrate
        energy
      }
    }
  }
}
