mutation createRecipe($input: CreateRecipeInput!) {
  createRecipe(input: $input) {
    id
    name
    description
    weightCooked
    recipeIngredients {
      id
      weight
      ingredient {
        id
        name
        protein
        fat
        carbonhydrate
        energy
      }
    }
  }
}
