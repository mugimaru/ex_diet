query listIngredients($cursor: String, $first: Int!) {
  listIngredients(first: $first, after: $cursor) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        name
        protein
        fat
        carbonhydrate
        energy
      }
    }
  }
}
